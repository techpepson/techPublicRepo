function darkMode() {
    // This is the dark mode screen logic
  
    const sunIcons = document.querySelectorAll('#sun-icons');

    const isDarkMode = false;

    sunIcons.forEach((icons)=>{
        icons.addEventListener('click',()=>{
        })
    })
    
  }
  
  export default darkMode;
  